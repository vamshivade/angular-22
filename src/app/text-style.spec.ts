import { TextStyle } from './text-style';

describe('TextStyle', () => {
  it('should create an instance', () => {
    const directive = new TextStyle();
    expect(directive).toBeTruthy();
  });
});
